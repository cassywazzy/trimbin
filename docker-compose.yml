services:
  trimbin:
    build: .
    container_name: trimbin
    restart: unless-stopped
    ports:
      - "${PORT:-5380}:5380"
    volumes:
      - trimbin_data:/data
    env_file:
      - .env

volumes:
  trimbin_data:
