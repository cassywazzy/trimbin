# Trimbin — environment variables
# Copy to .env and fill in your values

# Required — core
LETTERBOXD_USER=your-letterboxd-username
RADARR_URL=http://radarr:7878
RADARR_API_KEY=your-radarr-api-key

# Optional — qBittorrent integration (purge torrent listings on trim)
# When a movie/show is trimmed, the matching torrent is removed from qBit.
# Point this at your main qBit instance (not MAM-specific).
QBIT_URL=http://qbittorrent:8080
QBIT_USERNAME=admin
QBIT_PASSWORD=

# Optional — Simkl integration (watched movies + shows + anime)
# Create an app at https://simkl.com/settings/developer/new/ to get client_id.
# Then run the PIN auth flow to get an access token (tokens never expire):
#   1. Visit https://api.simkl.com/oauth/pin?client_id=YOUR_CLIENT_ID
#   2. Go to https://simkl.com/pin/ and enter the code
#   3. Poll the verification URL to get your access_token
SIMKL_CLIENT_ID=
SIMKL_ACCESS_TOKEN=

# Optional — Trakt integration (extra watch source for movies + shows).
# Only activates when both are set. Create an API app at https://trakt.tv/oauth/applications
TRAKT_CLIENT_ID=
TRAKT_USERNAME=

# Optional — Sonarr integration (show trimming)
SONARR_URL=http://sonarr:8989
SONARR_API_KEY=

# Optional — separate browser-facing URLs for the Radarr/Sonarr buttons in the UI.
# Set these if the API URL above is a Docker-internal name (e.g. http://radarr:7878)
# that your browser can't resolve; point them at the externally-reachable URL.
RADARR_BROWSER_URL=
SONARR_BROWSER_URL=

# Optional — Jellystat integration (multi-user watch counts)
# Requires Jellystat running alongside Jellyfin. Get your API key from Jellystat settings.
JELLYSTAT_URL=http://jellystat:3000
JELLYSTAT_API_KEY=

# Optional — Jellyfin integration (TMDB-to-Jellyfin ID mapping for Jellystat)
# Needed for Jellystat watch counts. Get an API key from Jellyfin > Dashboard > API Keys.
JELLYFIN_URL=http://jellyfin:8096
JELLYFIN_API_KEY=

# Optional — storage scans, Explorer & cleanup (Duplicates / Trickplay / Cleanup / Explorer tabs)
# Comma-separated library paths AS SEEN INSIDE THE CONTAINER — these must match the
# media mount in docker-compose.yml. Required for storage scans + trimming from the Explorer.
MEDIA_LIBRARIES=/media/movies,/media/tv,/media/anime

# Optional — local LLM (Ollama) for the Explorer's AI keep/delete recommendations
OLLAMA_URL=http://ollama:11434
OLLAMA_MODEL=llama3.1:8b
OLLAMA_TEMPERATURE=0.3
OLLAMA_TIMEOUT=60

# Optional — Letterboxd auto-ignore: skip films you liked / rated >= threshold (0.5–5.0)
LB_AUTO_IGNORE_LIKED=false
LB_MIN_RATING_IGNORE=

# Optional — notifications and monitoring
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your-webhook
DIGEST_TIME=21:00
DATA_DIR=/data
HC_PING_URL=https://healthchecks.example.com/ping/your-uuid
PORT=5380
